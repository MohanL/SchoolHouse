<%= bootstrap_form_for @supply do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :amount %>
  <% if @supply.student_class == nil %>
    <%= f.collection_select :student_class_id, StudentClass.all, :id, :class_name %>
  <% end %>
  <%= f.submit %>
<% end %>
