<%= bootstrap_form_for @supply, :remote => true do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :amount %>
  <%= f.collection_select :student_class_id, StudentClass.all, :id, :class_name %>
  <%= f.submit %>
<% end %>
